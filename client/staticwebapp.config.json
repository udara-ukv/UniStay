{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/*.png", "/*.jpg", "/*.jpeg", "/*.gif", "/assets/*"]
  },
  "routes": [
    {
      "route": "/api/*",
      "allowedRoles": ["anonymous"]
    }
  ]
}
